{
  "name": "snip-swap",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test:unit": "vitest",
    "lint": "prettier --plugin-search-dir . --check .",
    "format": "prettier --plugin-search-dir . --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.32.3",
    "@sveltejs/adapter-auto": "^2.0.0",
    "@sveltejs/kit": "^1.15.7",
    "@unocss/preset-icons": "^0.51.6",
    "@unocss/preset-tagify": "^0.51.6",
    "prettier": "^2.8.7",
    "prettier-plugin-svelte": "^2.10.0",
    "svelte": "^3.58.0",
    "svelte-check": "^3.2.0",
    "tslib": "^2.5.0",
    "typescript": "^5.0.4",
    "unocss": "^0.51.6",
    "vite": "^4.3.1",
    "vitest": "^0.30.1"
  },
  "type": "module",
  "dependencies": {
    "@lucia-auth/oauth": "^1.0.0",
    "@unocss/extractor-svelte": "^0.51.6",
    "lucia-auth": "^1.3.0"
  }
}
